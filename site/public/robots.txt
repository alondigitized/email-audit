User-Agent: *
Allow: /

# proxy.ts redirects unauthed requests to /login, so crawlers never reach
# our internal product surfaces. We don't enumerate /audits, /chat, /admin
# etc. here because that would just hand a route map to anyone running
# `curl /robots.txt`.
