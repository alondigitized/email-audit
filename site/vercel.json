{
  "ignoreCommand": "bash -c 'if [ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ] && git cat-file -e $VERCEL_GIT_PREVIOUS_SHA 2>/dev/null; then git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD -- ./; else exit 1; fi'",
  "git": {
    "deploymentEnabled": {
      "vault-notes": false
    }
  }
}